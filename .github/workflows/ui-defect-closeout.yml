name: UI defect closeout

on:
  push:
    branches:
      - codex/issue-61-sourceaccess-t0

permissions:
  contents: write

concurrency:
  group: ui-defect-closeout-${{ github.ref }}
  cancel-in-progress: false

jobs:
  closeout:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/issue-61-sourceaccess-t0
          fetch-depth: 0
      - name: Apply targeted UI defect fixes
        run: bash scripts/apply-ui-defect-closeout.sh
