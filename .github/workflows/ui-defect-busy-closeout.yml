name: UI defect busy-state closeout

on:
  push:
    branches:
      - codex/issue-61-sourceaccess-t0

permissions:
  contents: write

concurrency:
  group: ui-defect-busy-closeout-${{ github.ref }}
  cancel-in-progress: false

jobs:
  closeout:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: codex/issue-61-sourceaccess-t0
          fetch-depth: 0
      - name: Tighten rendered busy-state assertions
        run: bash scripts/apply-ui-defect-busy-closeout.sh
