name: Publish T5 docs closeout

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  idle:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'publisher paused while replacing staged docs payload'
