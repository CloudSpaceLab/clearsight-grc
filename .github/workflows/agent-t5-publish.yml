name: Publish T5 closeout

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  idle:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'publisher paused while staging the documentation batch'
