# Fictional document preview decision

The approved [sample plan](../superpowers/plans/2026-09-08-demo-document-samples.md) lets a bank stakeholder inspect the shipped fictional files in the existing Forms and Vendors document browser. Its first useful result is seeing the original sample and its submission context. It does not complete a malware scan or evidence review.

The before-state is AVAILABLE-only Quick Look: unscanned files display **Safety check pending**, retain submission details and offer no content or download. That remains the behavior for genuine uploads. The approved addition is a server-computed capability for the exact shipped sample manifest in non-production demo mode, followed by full stored-byte verification at the existing protected content route.

The selected-file inspector and Quick Look use the existing warning Notice for **Demo check complete** and **No antivirus scan was performed**. The notice precedes content and download, identifies the fictional sample and says it cannot support a bank decision. File-status badges remain warning/neutral; no success colour, scan animation, timer or invented scan receipt is introduced. Existing icons orient file types only.

Desktop keeps the document beside its facts. Narrow layouts retain the existing full-screen dialog, stack facts below the document and wrap long filenames. The warning and download remain above the content at 320px and 390px. Escape closes the dialog and returns focus to the selected file; no new motion or blocking guide is added. PDF/image fetch, cancellation, length/media checks and blob cleanup remain shared, while Office files use download fallback.

Required states are an eligible sample preview, unsupported-format download, genuine pending, quarantined/deleted/unknown, changed content, access revoked, failed fetch/retry and close during fetch. Behavioral regressions cover those boundaries; browser scenarios cover eligible and pending states in Forms and Vendors at 1440px, 390px and 320px in both themes, plus the actual shipped insurance PDF at desktop in each theme. The initial render showed stale fixture context naming a security certificate for the office statement; the scenario now names the registered-office question and sample vendor review. No production fixture was changed.

Focused renders use `UI_EVIDENCE_SCOPE=demo-documents` with `web/scripts/capture-forms-evidence.mjs`, a dedicated `UI_EVIDENCE_DIR`, and the isolated evidence build. The 26-state fixture matrix proves presentation and recovery, not hosted stored responses or antivirus deployment. Sample installation and hosted acceptance remain later tasks.

The corrected local evidence directory is `web/ui-evidence-demo-preview-dates-20260908/`. Its scenario-only metadata follows the authored dates: office statement issued 2 September 2026 and next confirmation due 2 September 2027; insurance issued 1 April 2026 and ending 30 September 2026. Fixture upload/submission times are 8 September 2026, after both issue dates. Assertions check the date order and the dates rendered in File details. Persisted sample installation must use actual upload/submission times. The insurance file retains the deliberate Brooklane/Northstar vendor contradiction.

On 8 September 2026 the headless Chromium runtime reported PDF viewing unavailable: the insurance PDF therefore showed the explicit PDF-application download fallback in both themes. The scenario fetches its protected download URL with credentials and verifies the complete 2,414-byte shipped file and SHA-256. This is download-byte evidence, not a claim that an inline PDF page rendered. The office-statement PNG did render from the protected blob flow. The hosted browser must separately confirm its actual PDF capability.

A subsequent local check used the installed full Chromium channel (`148.0.7778.96`, headless mode) rather than the default headless-shell runtime. Its unmodified `navigator.pdfViewerEnabled` was true. The existing PDF scenarios passed in both themes; the native viewer displayed the one-page Service insurance schedule, and protected preview plus download retained the complete manifest-matching bytes. The parent inspected `pdf-native-ready-light.png` and `pdf-native-ready-dark.png` in the local document QA directory. These are additional local renders, not hosted evidence or changes to the 26-state default matrix. The first captures were premature because frame `readyState` can complete before the native PDF viewer finishes loading. Hosted verification must wait for the native viewer's visible loading indicator to disappear and confirm its page count before capturing proof; iframe existence alone is insufficient. The native viewer initially exposes a thumbnail sidebar and 40% zoom in the desktop pane; no custom PDF controls or viewer dependency was added.

The repeatable runner now waits for the actual Chromium PDF extension frame, visible viewer, hidden loading indicator and page 1 of 1 before capture. `UI_EVIDENCE_SCOPE=demo-documents-pdf` selects the two existing desktop PDF states; `UI_EVIDENCE_CHROMIUM_CHANNEL=chromium` opts into the installed full browser. Omit the channel for headless-shell fallback. The normal `demo-documents` scope still contains exactly 26 states. Use a freshly built `dist-evidence`, the existing managed preview helper and a separate output directory for each runtime. Preference initialization now runs only in the top frame, and the evidence harness answers its absent `/favicon.ico` with 204; application and PDF viewer errors still fail capture.

Fresh 8 September captures passed all four PDF cases, with no browser errors or horizontal overflow. The inspected files and manifests are retained under `C:/Users/Son/AppData/Local/Temp/clearsight-document-navigation-20260908/task4-final-chromium/` and `task4-final-fallback/`. Both runtimes report version `148.0.7778.96`: full Chromium records native viewing true, page 1/1 and two protected reads (preview plus download); headless shell records native viewing false and one download read, with no preview fetch. Every download is 2,414 bytes with SHA-256 `14520f835f3edb3a14c6e515ff1c470e93474f9d7641ccdef8a458f04571b2d4`. The native screenshots show the insurance page in both themes at the browser's initial 42% zoom, with the demo warning and download above it. The fallback screenshots retain the PDF-application recovery message. Hosted stored-response verification remains separate.

The browser-only pending PDF row now names `Supplier insurance schedule.pdf`; PNG pending rows keep `Supplier office statement.png`. Node regressions execute the installed fixture's response decoration, prevent early capture while viewer loading is pending, reject invalid page counts/loading failure and retain the 26-state matrix. They caught the original filename and early-readiness failures before correction. The existing CI `check:ui-contracts` command now includes these checks (44 passed); the isolated evidence build also passed. Shipped sample assets, production fixtures, viewer dependencies and inspection/acceptance behavior are unchanged.

Review and the first combined run caught an evidence-harness ordering regression: installing the favicon 204 response before recovery seeding aborted the recovery fixture's origin-initializing navigation. The exact favicon route now installs after seeding. The existing `forms-recovery-restored` scenario passed through the actual runner with only its scenario loop narrowed for the focused check; the recovered response and required photo reselection were inspected. Both native and both fallback PDF scenarios then passed again, as did all 44 UI contracts. The additional receipts are under the same local QA root in `task4-recovery-fix/`, `task4-recovery-fix-chromium/` and `task4-recovery-fix-fallback/`. The failed combined-run receipt is retained for the coordinated full rerun.

PR #205's first UI run on Chromium `140.0.7339.16` stopped in the existing document-preview resize assertion after 192 captures. Its failure did not retain the geometry, so the exact CI scheduling cannot be reconstructed. Local diagnostics with the same installed browser observed mobile layout changing between separate protocol reads. A controlled two-render-frame scheduling gap reproduced the false rejection: the old dialog top was 33px, the new Close top was 21px, while a simultaneous current snapshot put the dialog top at 1px and kept Close fully visible. The diagnostic image is `geometry-failure-0.png` in the same QA root. No active dialog animation or requested/actual viewport mismatch was present in that reproduction.

The recovery assertion now samples dialog, Close and actual viewport together and requires matching valid geometry in two consecutive rendered frames, bounded to ten seconds. Existing viewport bounds remain strict; missing, hidden and offscreen controls still reject, with safe requested/actual geometry in the error. The mixed-layout regression failed before correction and passed afterward; all 48 UI contracts passed. The actual evidence runner then passed all eight Forms/Vendors document-browser states using Chromium `140.0.7339.16`, including the failed desktop-dark resize path and real browser serialization of the Close element. Those receipts are in `geometry-fixed-chromium140/`. This changes evidence measurement only; application layout, copy and controls are unchanged. A fresh combined CI run remains required.

Quality review added deadline edge cases: no delivered animation frame and a second otherwise-valid frame arriving after ten seconds. Both failed before correction. An independent deadline timer now rejects missing frames, each frame checks the deadline before acceptance, and every completion cancels the timer and pending frame. All 50 UI contracts passed, including timely acceptance, strict geometry rejection and deadline cleanup. A fresh Chromium `140.0.7339.16` desktop-dark resize smoke passed through the actual runner; its receipt is `geometry-deadline-fixed-chromium140/` in the same QA root.
