# Fictional document preview decision

The approved [sample plan](../superpowers/plans/2026-09-08-demo-document-samples.md) lets a bank stakeholder inspect the shipped fictional files in the existing Forms and Vendors document browser. Its first useful result is seeing the original sample and its submission context. It does not complete a malware scan or evidence review.

The before-state is AVAILABLE-only Quick Look: unscanned files display **Safety check pending**, retain submission details and offer no content or download. That remains the behavior for genuine uploads. The approved addition is a server-computed capability for the exact shipped sample manifest in non-production demo mode, followed by full stored-byte verification at the existing protected content route.

The selected-file inspector and Quick Look use the existing warning Notice for **Demo check complete** and **No antivirus scan was performed**. The notice precedes content and download, identifies the fictional sample and says it cannot support a bank decision. File-status badges remain warning/neutral; no success colour, scan animation, timer or invented scan receipt is introduced. Existing icons orient file types only.

Desktop keeps the document beside its facts. Narrow layouts retain the existing full-screen dialog, stack facts below the document and wrap long filenames. The warning and download remain above the content at 320px and 390px. Escape closes the dialog and returns focus to the selected file; no new motion or blocking guide is added. PDF/image fetch, cancellation, length/media checks and blob cleanup remain shared, while Office files use download fallback.

Required states are an eligible sample preview, unsupported-format download, genuine pending, quarantined/deleted/unknown, changed content, access revoked, failed fetch/retry and close during fetch. Behavioral regressions cover those boundaries; browser scenarios cover eligible and pending states in Forms and Vendors at 1440px, 390px and 320px in both themes, plus the actual shipped insurance PDF at desktop in each theme. The initial render showed stale fixture context naming a security certificate for the office statement; the scenario now names the registered-office question and sample vendor review. No production fixture was changed.

Focused renders use `UI_EVIDENCE_SCOPE=demo-documents` with `web/scripts/capture-forms-evidence.mjs`, a dedicated `UI_EVIDENCE_DIR`, and the isolated evidence build. The 26-state fixture matrix proves presentation and recovery, not hosted stored responses or antivirus deployment. Sample installation and hosted acceptance remain later tasks.

The corrected local evidence directory is `web/ui-evidence-demo-preview-dates-20260908/`. Its scenario-only metadata follows the authored dates: office statement issued 2 September 2026 and next confirmation due 2 September 2027; insurance issued 1 April 2026 and ending 30 September 2026. Fixture upload/submission times are 8 September 2026, after both issue dates. Assertions check the date order and the dates rendered in File details. Persisted sample installation must use actual upload/submission times. The insurance file retains the deliberate Brooklane/Northstar vendor contradiction.

On 8 September 2026 the headless Chromium runtime reported PDF viewing unavailable: the insurance PDF therefore showed the explicit PDF-application download fallback in both themes. The scenario fetches its protected download URL with credentials and verifies the complete 2,414-byte shipped file and SHA-256. This is download-byte evidence, not a claim that an inline PDF page rendered. The office-statement PNG did render from the protected blob flow. The hosted browser must separately confirm its actual PDF capability.
