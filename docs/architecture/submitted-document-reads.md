# Submitted document reads

This architecture note adds read indexes, not table ownership entries. Existing capture and third-party tables remain registered once in the durable schema ownership register.

Evidence/Capture owns the shared submitted-document read model. It adds no document table: an occurrence is an immutable response revision (when present), submission, field and artifact. `capture_artifacts.submission_id` records first use and is not later-revision membership. Files uploaded by another contributor must belong to the same exact distribution and legal entity.

Migration `000081_submitted_document_reads` adds reverse submission/revision, submitted-time and assessment-request-link indexes. Existing request/entity/form, artifact primary-key, work-capture and relationship indexes remain authoritative. The shared authority predicate is extracted from the existing vendor-work list and remains a read predicate, not command authorization.

PostgreSQL joins the verified tenant and exact legal entity, immutable submissions, form fields, artifact IDs and subject access before LIMIT. Program and Matter access uses the existing subject predicate. Unknown subject types fail closed. Assessment and vendor-work captures use their stored request links and exact origin/version; their relationship, read routes and target access are checked before pagination. Request links permit reading submitted history even before a third-party response reaction is processed. Draft artifacts have no submission occurrence. The memory adapter delegates legacy access to the owning workflow services and applies the same occurrence filter.

Assessment review metadata comes only from `third_party_documents`. It is shown for its original submitted occurrence; reuse in a later revision does not transfer acceptance. Business rejection and expiry do not deny historical read access when the stored bytes are AVAILABLE. No document mutation or AI validation rule is introduced.

Assessment and vendor-work request links also require exact template ID and version equality with their workflow record. Currency is per submitted field replacement across linked capture sequences, not the mutable current-request pointer or a successor distribution's local revision flag. Within a capture sequence, response revision and submitted time identify the latest source. A later submitted request clears each of its requested fields before applying its answers; unrelated fields retain their earlier source. Pending requests do not retire evidence. This calculation precedes the current-only filter and LIMIT in PostgreSQL; the memory adapter mirrors it.

## Bounds and retention

List limits are 1–100 (default 25), filename search is at most 200 bytes, and cursors are at most 2048 bytes. Pagination orders by submitted time and stable occurrence ID descending. Exact content queries reuse the same scoped inventory with submission/field/artifact selectors and an optional response revision. UUID selectors remain bound, and artifact joins use the UUID primary key after validating the submitted reference.

The inventory retains no derived document content, tokens or storage keys. Original manifests and submitted answers retain their existing retention obligations. No deletion, retention period or partition policy is added by these indexes. Deployment sizing should budget for 10,000 submissions × 20 file occurrences per entity as an initial 200,000-occurrence scenario; tenant/entity filtering and exact form/vendor scope reduce the working set. Production-scale p95 and concurrency benchmarks for that scenario remain a release gate; the integration checks in this tranche prove exact access, pagination and linked legacy reads, not a completed high-volume benchmark.

The focused PostgreSQL regression also reuses original and contributor-uploaded artifacts across two immutable revisions. Its executable `EXPLAIN (ANALYZE, BUFFERS)` exact-response query on the small fixture returned two rows under a Limit in 2.325 ms with 66 shared-buffer hits on 2026-09-07. Observed access included response-revision, artifact tenant/ID, reverse submission/revision, assessment-request-link and work-capture indexes. This is representative query-path evidence only, not production cardinality or latency acceptance.

OpenArtifact buffers at most the configured upload size, checks exact size and SHA-256, and only then returns content. This protects reads from mutable development storage. The API delivers PDF and supported raster images inline; other formats use attachment disposition. Every content response uses private/no-store, nosniff and a sandbox content policy. Missing, changed, unscanned, quarantined and unknown artifacts return no bytes. Production object-storage deployment remains separate work.

## Vendor request collection receipts

An assessment reviewer may select an existing vendor-submitted (`MAGIC_LINK`) document occurrence for a requested `vendor_document` field. `POST /api/v1/vendor-assessments/{id}/collection/{field_id}/reconcile` takes assessment and request versions, the exact source submission/field/artifact identifiers, optional response revision, and a relevance rationale. Both current reviewer authority and source inventory access are checked. The source must match the assessment relationship and requested file constraints. The receipt clears the vendor collection obligation; it does not create a respondent answer, submission, or bank acceptance.

`POST /api/v1/vendor-assessments/{id}/prepare-request` takes `expected_version`, `audience`, and `deadline`. It prepares the real capture request without opening a route or sending an invitation. The existing send command reuses it; an empty audience resumes the sealed prepared recipient, without returning the address. `GET /api/v1/vendor-assessments/{id}/collection` returns the field checklist, assessment/request versions, server capabilities, stored deadline, masked audience hint, vendor pending count, bank pending count, and accepted document count. Scalar answers have `bank_review_state: NOT_REQUIRED`; only explicit document decisions count as accepted documents.

The new authoritative `capture_field_collection_resolutions` ledger is append-only. Current capture fields reference their latest receipt; each receipt retains the chosen source, actor, rationale, source digest and size, predecessor, and independent bank decision. The assessment row, request version, receipt, domain event, outbox event, and existing required maintenance participate in one PostgreSQL transaction. Migration 85 adds `collection_completed_at`: when all required items are already held with no unresolved condition, review can begin with no vendor submission. This marker must never be interpreted as `submitted_at`.

The existing document review command accepts optional `field_id`; it is required when one artifact satisfies several requirements. Each receipt requires explicit bank validation. Rejection, expiry, source replacement, loss of integrity, or unavailable inspection removes live collection fulfillment while retaining historical receipts and decisions. Capture validation and reminder/progress reads recheck those source conditions. Receipt metadata is redacted from respondent request DTOs; only the safe `collection_received` indication remains. Protected receipt previews repeat exact source inventory authorization before opening the original artifact.

Executable coverage includes request preparation and send reuse, actual HTTP identity/scope binding, independent receipt review when an artifact is shared, source file constraints, no fabricated vendor submission, and real PostgreSQL atomic write/version denial, immutable history, source rejection, bank acceptance, and completion checks. The focused test database is PostgreSQL 18.6. Production volume and concurrency benchmarks remain a release gate, as for the existing submitted-document inventory.
